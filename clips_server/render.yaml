services:
  - type: web
    name: ephone-clips
    env: node
    buildCommand: apt-get install -y ffmpeg && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
